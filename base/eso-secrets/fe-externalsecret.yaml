apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-secret-fe
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: myprofile-secretstore
    kind: SecretStore
  target:
    name: myprofile-frontend-env-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: app/dev/frontend/env
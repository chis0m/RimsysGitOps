---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myprofile-backend
spec:
  selector:
    matchLabels:
      app: myprofile-backend
  template:
    metadata:
      labels:
        app: myprofile-backend
    spec:
      initContainers:
        - name: myprofile-backend-migration
          image: 139080726045.dkr.ecr.us-east-1.amazonaws.com/backend:aae63f030dc9b113c6beb383becd3e7fd1f9b035
          command: ['sh', '-c', 'php artisan migrate:fresh --seed']
          envFrom:
            - configMapRef:
                name: myprofile-backend-env-config

      containers:
        - name: myprofile-backend
          image: 139080726045.dkr.ecr.us-east-1.amazonaws.com/backend:aae63f030dc9b113c6beb383becd3e7fd1f9b035
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 400Mi
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: myprofile-backend-env-config